import ButtonKey from '../button';
test('test', () => {
    expect(true).toBe(true);
})

function pressingButtons(button) {

    <ButtonKey value={5}></ButtonKey>
}

test('Calculator add 3+2 should give 5', () => {
    let value = 0

})